[build]
builder = "nixpacks"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[environment]
PYTHONUNBUFFERED = "1"
TESSDATA_PREFIX = "/nix/var/nix/profiles/default/share/tessdata"
